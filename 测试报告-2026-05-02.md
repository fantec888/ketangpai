# 课堂派项目测试报告

测试时间：2026-05-02 17:43-17:53  
测试环境：Windows 本机，前端 `http://127.0.0.1:3000/`，后端 `http://127.0.0.1:8090`，数据库 `ktp`

## 测试结论

本次修改后的核心功能通过验证：

- 置顶课程卡片不再遮挡课程信息，卡片高度和外层面板已调整。
- 学生作业列表评论数已从 `/api/task/getComments` 实时统计，不再固定显示 `评论 0`。
- 消息通知已接入待办数据，顶部铃铛统计为“未读消息 + 待办作业”，消息页会展示待提交/待批改卡片并支持跳转。
- 客服/公众号二维码静态资源可访问。

发现并处理的问题：

- 命令行环境存在 `HTTP_PROXY/HTTPS_PROXY=http://127.0.0.1:7890`，PowerShell 的部分请求会误走代理，导致本地 `/home`、`/message` 测试出现假 404/502。使用 `curl --noproxy "*"` 直连测试正常。
- 已在 `vue.config.js` 增加 `historyApiFallback: true`，用于提升 Vue history 路由直达/刷新兼容性。

## 构建测试

| 项目 | 命令 | 结果 | 备注 |
| --- | --- | --- | --- |
| 前端 | `npm.cmd run build` | 通过 | 仅有资源体积 warning，不影响运行 |
| 后端 | `mvnw.cmd -DskipTests package` | 通过 | 生成 Spring Boot jar 成功 |

前端 warning：

- `chunk-vendors` 约 935 KiB。
- 入口资源总大小约 1.13 MiB。

这些是性能提示，不是编译错误。

## 冒烟测试

样本数据：

- 学生：`5f947dc6`
- 教师：`e311f7c2`
- 课程：`cacff`
- 作业：`48893b`、`efc2b9`

| 测试项 | 结果 | 说明 |
| --- | --- | --- |
| 前端根路径 `/` | 通过 | 返回 200 |
| 前端 `/home`、`/message` | 通过 | 使用直连绕过代理返回 200 |
| 二维码 `/static/img/contact-qr.jpg` | 通过 | 返回 200，大小 167081 字节 |
| 学生课程列表 | 通过 | 返回 1 门课程 |
| 教师课程列表 | 通过 | 返回 2 门课程 |
| 课程详情 | 通过 | `cacff` 可查询 |
| 作业列表 | 通过 | `cacff` 返回 2 个作业 |
| 作业评论 | 通过 | `efc2b9` 返回 1 条评论 |
| 成绩查询 | 通过 | 学生作业状态可查询 |
| 消息列表 | 通过 | 当前学生消息表返回 0 条 |
| 未读消息数 | 通过 | 当前学生未读数为 0 |

## 数据一致性检查

| 作业 | tno | 评论数 | 学生提交状态 | 教师待批数 |
| --- | --- | ---: | ---: | ---: |
| 2 | `48893b` | 1 | 0 | 0 |
| 111 | `efc2b9` | 1 | 1 | 1 |

解释：

- 学生端作业列表应显示两个作业的真实评论数均为 `1`。
- 学生 `5f947dc6` 对作业 `48893b` 状态为 `0`，会进入待提交待办。
- 作业 `efc2b9` 的 `submit_num - judge_num = 1`，教师端会进入待批改待办。
- 学生消息表当前为空，所以消息通知页中的有效提醒主要来自待办。

## 轻量压力测试

方式：对 11 个只读接口/静态资源进行并发请求。  
规模：每个目标 30 次，共 330 次请求，最大并发约 12。  
工具：`curl.exe --noproxy "*"`，避免本机代理干扰。

| 目标 | 总数 | 成功 | 失败 | 平均耗时 ms | P95 ms | 最大 ms |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| comments | 30 | 30 | 0 | 3.00 | 3.33 | 3.52 |
| front-home | 30 | 30 | 0 | 3.97 | 4.40 | 17.74 |
| front-message | 30 | 30 | 0 | 5.07 | 18.86 | 21.46 |
| front-root | 30 | 30 | 0 | 2.42 | 2.66 | 16.28 |
| grade | 30 | 30 | 0 | 3.08 | 3.45 | 3.45 |
| messages | 30 | 30 | 0 | 3.71 | 3.94 | 22.52 |
| qr-asset | 30 | 30 | 0 | 2.65 | 2.77 | 13.64 |
| student-courses | 30 | 30 | 0 | 3.64 | 3.76 | 19.44 |
| tasks | 30 | 30 | 0 | 3.50 | 4.59 | 5.17 |
| teacher-courses | 30 | 30 | 0 | 3.75 | 3.69 | 23.67 |
| unread-count | 30 | 30 | 0 | 3.39 | 3.73 | 15.23 |

压力测试结论：轻量并发下没有发现接口失败、超时或明显性能异常。

## 断点测试

方式：使用 Microsoft Edge headless 在不同视口宽度下生成截图，验证页面能否在常见断点正常加载。  
截图目录：

- 未登录/路由守卫断点截图：`ketangpai/breakpoint-shots`
- 登录态关键页面断点截图：`ketangpai/breakpoint-shots-auth`

未登录断点测试：

| 页面 | 断点 | 结果 |
| --- | --- | --- |
| `/home` | 1920x1080 | 通过 |
| `/home` | 1440x900 | 通过 |
| `/home` | 1024x768 | 通过 |
| `/home` | 768x1024 | 通过 |
| `/home` | 430x932 | 通过 |
| `/home` | 390x844 | 通过 |
| `/home` | 375x812 | 通过 |
| `/message` | 1440x900 | 通过 |
| `/message` | 768x1024 | 通过 |
| `/message` | 390x844 | 通过 |

说明：未登录状态会被路由守卫重定向到登录页，该组主要验证路由守卫和登录页在不同断点可正常加载。

登录态断点测试：

| 页面 | 断点 | 结果 | 截图大小 |
| --- | --- | --- | ---: |
| `/home` | 1440x900 | 通过 | 167365 bytes |
| `/home` | 1024x768 | 通过 | 111588 bytes |
| `/home` | 768x1024 | 通过 | 179118 bytes |
| `/home` | 430x932 | 通过 | 92692 bytes |
| `/home` | 390x844 | 通过 | 69202 bytes |
| `/home` | 375x812 | 通过 | 64052 bytes |
| `/message` | 1440x900 | 通过 | 112869 bytes |
| `/message` | 768x1024 | 通过 | 72395 bytes |
| `/message` | 390x844 | 通过 | 46152 bytes |
| `/studenttask/cacff` | 1440x900 | 通过 | 149320 bytes |
| `/studenttask/cacff` | 768x1024 | 通过 | 115281 bytes |
| `/studenttask/cacff` | 390x844 | 通过 | 55482 bytes |

断点测试结论：登录后的首页、消息页和学生作业页在桌面、平板、手机常见宽度下都能正常加载并生成有效截图。移动端宽度下内容会纵向排列，未发现页面空白或资源加载失败。

## 风险与建议

- 前端体积偏大，后续上线可以考虑路由懒加载拆包、关闭无用依赖、启用 gzip。
- 消息通知中的待办目前是前端聚合多个接口计算，数据量大时会产生较多请求。正式上线建议后端新增一个 `/notification/list` 或 `/todo/list` 聚合接口。
- 本地命令行代理会干扰本机地址测试。测试本地服务时建议临时设置 `NO_PROXY=127.0.0.1,localhost`，或使用 `curl --noproxy "*"`。
- 后端 Maven 本次使用 `-DskipTests`，项目当前没有可用自动化测试用例。建议后续为作业、评论、消息通知补基础单元/接口测试。
